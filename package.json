{
  "name": "nft-valuations-admin-panel",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "compile:dev": "tsc -w",
    "start": "npm run compile && node dist/server.js",
    "prod": "npm run compile && npm rebuild --verbose sharp && node dist/server.js",
    "start:dev": "nodemon"
  },
  "dependencies": {
    "axios": "^1.3.6",
    "bullmq": "^3.12.0",
    "express": "^4.18.2",
    "redis": "^4.6.5",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "nodemon": "^2.0.20",
    "ts-jest": "^29.0.3",
    "ts-node": "^10.9.1"
  }
}
